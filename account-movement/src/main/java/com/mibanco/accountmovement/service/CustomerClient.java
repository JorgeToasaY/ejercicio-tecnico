/*
package com.mibanco.accountmovement.service;

import com.mibanco.accountmovement.dto.CustomerResponseDTO;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

@FeignClient(name = "customer-person", url = "http://localhost:8080")
public interface CustomerClient {

    @GetMapping("/api/clientes/{customerId}")
    CustomerResponseDTO getCustomerById(@PathVariable("customerId") String customerId);
}*/
